## Rule to install new tool or dependencies

DO NOT assume the latest version number from your knowledge base, always search web to find out the actual latest method to install or latest version.

## Record weirdness

When something weird happens, please record it "Debug.md" so that the future agents can refer to when the same issue happens. Each issue should include title/description/solution/side-note(if need).
